{
  "openapi": "3.1.0",
  "info": {
    "title": "Tout Pris API",
    "version": "0.1.0",
    "description": "Backend API of the Tout Pris project."
  },
  "paths": {
    "/api/health": {
      "get": {
        "operationId": "tout_pris_api_health",
        "summary": "Health check",
        "parameters": [],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthSchema"
                }
              }
            }
          }
        },
        "tags": [
          "monitoring"
        ]
      }
    }
  },
  "components": {
    "schemas": {
      "HealthSchema": {
        "properties": {
          "status": {
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "status"
        ],
        "title": "HealthSchema",
        "type": "object"
      }
    }
  },
  "servers": []
}